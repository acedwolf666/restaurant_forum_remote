<ul>

  <% @users.each do |user| %>
    <li>
      <%= user.name %>
      <%= link_to 'Follow', followships_path(followed_id: user), method: :post %>
      <%= link_to 'Unfollow', followship_path(user), method: :delete %>
    </li>
  <% end %>

</ul>
